#include "main.h"

int main(void)
{
    int number;

    number = -5;
    _putchar(_abs(number) + '0');
    _putchar('\n');

    number = 0;
    _putchar(_abs(number) + '0');
    _putchar('\n');

    number = 7;
    _putchar(_abs(number) + '0');
    _putchar('\n');

    return (0);
}

